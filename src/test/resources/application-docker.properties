# =========================================================
# Свойства ТОЛЬКО для Docker-интеграционных тестов (Flyway на реальном Postgres)
# НЕ содержит H2, НЕ выключает Testcontainers.
# Активируется профилем @ActiveProfiles("docker") в тесте.
# =========================================================

jwt.secret=dockerTestSecretKeyAtLeast32CharactersLongForIntegration
jwt.expiration-ms=86400000